# PostgreSQLとは
オープンソースで、高機能かつ堅牢なRDBMS。

## ポート
5432

## デフォルトデータベース
postgres

## デフォルトユーザー
postgres

## 接続方法
```bash
psql -h <ターゲット> -U <ユーザー名> -p <ポート>
```

## コマンド
```bash
\l # データベースを表示
\c <データベース名> # データベースを切り替え
\dt # テーブルを表示
```
