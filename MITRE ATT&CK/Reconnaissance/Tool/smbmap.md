# smbmapとは
ネットワーク上のSMB/CIFS（ファイル、プリンター共有などに使われるプロトコル）共有を探索・監査するためのPython製のコマンドラインツール。

## コマンド
```bash
smbmap -H <ターゲット> -u guest -p "" # 匿名アクセス
smbmap -H <ターゲット> -u <ユーザー名> -p "<パスワード>" # 認証ユーザーでアクセス
smbmap -H <ターゲット> -u <ユーザー名> -p "<パスワード>" -r <共有フォルダ名> # 認証ユーザーで共有フォルダ内にアクセス
```
